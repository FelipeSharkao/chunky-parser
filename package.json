{
  "name": "nasin",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "bun run nx run-many --target=build",
    "build:dev": "NODE_ENV=development bun run nx run-many --target=build",
    "test": "bun run nx affected --target=test",
    "test:dev": "NODE_ENV=development bun run nx affected --target=test"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "nx": "15.0.11",
    "@trivago/prettier-plugin-sort-imports": "^3.2.0",
    "tslib": "^2.4.0",
    "bun-types": "^0.1.4",
    "typescript": "^4.6.4",
    "prettier": "^2.6.2"
  }
}
